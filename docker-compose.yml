version: "3.1"
services:
 monitor-service:
   image: monitor-service:latest   
   ports:
     - "9889:9889"
   volumes:
     - /home/fluentadm/docker-service/logs:/logs
   networks:
     - dockernet
networks:
 dockernet:
   external: true